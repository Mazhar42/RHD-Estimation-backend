# Render service configuration for FastAPI backend
services:
  - type: web
    name: rhd-estimation-backend
    env: python
    rootDir: backend
    buildCommand: "./build.sh"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    autoDeploy: true